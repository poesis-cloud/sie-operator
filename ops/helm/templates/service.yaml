apiVersion: v1
kind: Service
metadata:
  name: {{ include "sie-causal-processor.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "sie-causal-processor.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http

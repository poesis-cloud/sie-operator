apiVersion: v1
kind: Service
metadata:
  name: {{ include "sie-operator.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "sie-operator.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http

{
  "$schema": "gsmarc://gsm/Archetype/v1",
  "$id": "gsmarc://ops/OperationResponse/v1",
  "title": "OperationResponse",
  "description": "SIE Operator effector archetype — operation response contract. Defines the shape of an operation outcome emitted by the operator's run-operation mechanism.",
  "type": "object",
  "required": [ "success" ],
  "properties": {
    "success": {
      "type": "boolean",
      "description": "Whether the operation completed successfully."
    },
    "effects": {
      "type": "array",
      "description": "Effects produced by the mechanism rule execution.",
      "items": {
        "type": "object"
      }
    },
    "error": {
      "type": [ "string", "null" ],
      "description": "Error message when the operation failed."
    }
  },
  "unevaluatedProperties": false
}
# Kafka Protocol

> **Status**: Reserved — not yet implemented.

Asynchronous event streaming protocol for dispatching effects to external Kafka brokers. Will follow the same Archetype Quad pattern as HTTP and Relay.

Anticipated archetype family: `KafkaEvent` (Data), `KafkaEventEffector`, `KafkaEventReceptor`, `KafkaEventInteraction`.

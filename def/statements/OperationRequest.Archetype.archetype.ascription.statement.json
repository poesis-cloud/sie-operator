{
  "$schema": "gsm://archetypes/Archetype/v1",
  "$id": "sie-operator://archetypes/OperationRequest/v1",
  "title": "OperationRequest",
  "description": "SIE Operator receptor archetype — operation request contract. Defines the shape of an operation trigger received by the operator's run-operation mechanism. Client mechanisms wire an Effector to this receptor via an Interaction to declare operability.",
  "type": "object",
  "required": [ "mechanismAscriptionId", "triggerPayload" ],
  "properties": {
    "mechanismAscriptionId": {
      "type": "string",
      "format": "uuid",
      "$gsm:queryable": true,
      "description": "The ascription ID of the client mechanism to execute."
    },
    "triggerPayload": {
      "type": "object",
      "description": "The trigger payload — validated against the mechanism's trigger receptor archetype schema at execution time."
    }
  },
  "unevaluatedProperties": false
}
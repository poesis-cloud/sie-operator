name: cp-cd

on:
  workflow_dispatch:
    inputs:
      target:
        description: "Deployment target"
        required: true
        type: choice
        options:
          - aks
          - onprem
      environment:
        description: "Deployment environment"
        required: true
        type: choice
        options:
          - preprod
          - prod

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  package:
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - name: Build jar
        run: mvn -B -DskipTests package
      - name: Generate SBOM
        run: mvn -B cyclonedx:makeAggregateBom
      - name: Build image
        run: docker build -t ghcr.io/${{ github.repository_owner }}/sie-causal-processor:${{ github.sha }} .
      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Push image
        run: docker push ghcr.io/${{ github.repository_owner }}/sie-causal-processor:${{ github.sha }}
      - name: Sign image (keyless)
        uses: sigstore/cosign-installer@v3
      - name: Cosign sign
        run: cosign sign --yes ghcr.io/${{ github.repository_owner }}/sie-causal-processor:${{ github.sha }}
      - name: Set output
        id: meta
        run: echo "image_tag=${GITHUB_SHA}" >> "$GITHUB_OUTPUT"

  prod-deploy:
    if: ${{ github.event.inputs.target == 'aks' }}
    needs: package
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    steps:
      - uses: actions/checkout@v4
      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - name: Set AKS context
        uses: azure/aks-set-context@v4
        with:
          resource-group: ${{ secrets.AKS_RESOURCE_GROUP }}
          cluster-name: ${{ secrets.AKS_CLUSTER_NAME }}
      - name: Deploy with Makefile
        run: |
          make prod-deploy \
            DEPLOY_ENV=${{ github.event.inputs.environment }} \
            IMAGE_REPOSITORY=ghcr.io/${{ github.repository_owner }}/sie-causal-processor \
            IMAGE_TAG=${{ needs.package.outputs.image_tag }}

  deploy-onprem:
    if: ${{ github.event.inputs.target == 'onprem' }}
    needs: package
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    steps:
      - uses: actions/checkout@v4
      - name: Deploy on-prem
        run: echo "On-prem deployment not yet implemented"
